const OtpTypes = Object.freeze({
    ONBOARDING: 1,
})

const SubscriptionPlans = Object.freeze({
    BASIC: 1, 
    PREMIUM: 2, 
    STANDARD: 3
})

module.exports = {
    OtpTypes, 
    SubscriptionPlans
}